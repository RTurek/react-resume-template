import React from "react"
import styled from "styled-components";

const MainColumnContainer = styled.section`
  height: 100%;
  width: 70%;
  margin: auto;
`;

export default function Resume() {
  return (
    <MainColumnContainer>
      MainColumn
    </MainColumnContainer>
  );
}
